version: "3.8"

services:
  graphdb:
    image: ontotext/graphdb:10.6.2
    container_name: agentkg-graphdb
    restart: unless-stopped
    ports:
      - "127.0.0.1:7200:7200"
    volumes:
      - ./graphdb-data:/opt/graphdb/home
    environment:
      - GDB_HEAP_SIZE=2g
