export async function syncAgentCardsForChain(chainId: number, _opts?: { force?: boolean; limit?: number }): Promise<void> {
  console.info(`[sync] [agent-cards] chainId=${chainId} disabled (performance optimization)`);
}

